server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bibliobook_db
    username: postgres
    password: P@ssw0rd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true